export enum Direction {
  Up = 1,
  Down,
  Left,
  Right,
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  UNKNOWN = 'unknown',
  DEPRECATED_OLD = 'deprecated_old',
  REMOVED_OLD = 'removed_old',
}
